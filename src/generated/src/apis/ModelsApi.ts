/* tslint:disable */
/* eslint-disable */
/**
 * Fetch Hive Public API
 * The Fetch Hive public API lets you invoke prompts, workflows, agents, and Hive Agents programmatically using an API token.  ## Authentication All requests require a Bearer token in the `Authorization` header: ``` Authorization: Bearer <your-api-token> ```  ## Streaming Pass `\"streaming\": true` in the request body to receive a `text/event-stream` response. Each event is a JSON object sent as `data: <json>\\n\\n`. The stream ends with `data: [DONE]\\n\\n`. 
 *
 * The version of the OpenAPI document: 0.2.8
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type ErrorResponse,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
} from '../models/ErrorResponse';
import {
    type GetPublicModels200ResponseInner,
    GetPublicModels200ResponseInnerFromJSON,
    GetPublicModels200ResponseInnerToJSON,
} from '../models/GetPublicModels200ResponseInner';

/**
 * ModelsApi - interface
 * 
 * @export
 * @interface ModelsApiInterface
 */
export interface ModelsApiInterface {
    /**
     * Creates request options for getPublicModels without sending the request
     * @throws {RequiredError}
     * @memberof ModelsApiInterface
     */
    getPublicModelsRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Returns all active (non-deprecated) LLM and image-generation models available in Fetch Hive as a flat array. Excludes embedding-only models.  `provider` identifies the underlying model maker (e.g. `openai`, `anthropic`, `minimaxai`).  `model_type` is `\"llm\"` for chat/text models and `\"image_generation\"` for models that generate images. `is_vision` means a model accepts image input; `is_image_generation` means a model generates images.  `is_reasoning` is `true` when the model supports reasoning capabilities. 
     * @summary List active models
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ModelsApiInterface
     */
    getPublicModelsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<GetPublicModels200ResponseInner>>>;

    /**
     * Returns all active (non-deprecated) LLM and image-generation models available in Fetch Hive as a flat array. Excludes embedding-only models.  `provider` identifies the underlying model maker (e.g. `openai`, `anthropic`, `minimaxai`).  `model_type` is `\"llm\"` for chat/text models and `\"image_generation\"` for models that generate images. `is_vision` means a model accepts image input; `is_image_generation` means a model generates images.  `is_reasoning` is `true` when the model supports reasoning capabilities. 
     * List active models
     */
    getPublicModels(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<GetPublicModels200ResponseInner>>;

}

/**
 * 
 */
export class ModelsApi extends runtime.BaseAPI implements ModelsApiInterface {

    /**
     * Creates request options for getPublicModels without sending the request
     */
    async getPublicModelsRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearer_auth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/public/models`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns all active (non-deprecated) LLM and image-generation models available in Fetch Hive as a flat array. Excludes embedding-only models.  `provider` identifies the underlying model maker (e.g. `openai`, `anthropic`, `minimaxai`).  `model_type` is `\"llm\"` for chat/text models and `\"image_generation\"` for models that generate images. `is_vision` means a model accepts image input; `is_image_generation` means a model generates images.  `is_reasoning` is `true` when the model supports reasoning capabilities. 
     * List active models
     */
    async getPublicModelsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<GetPublicModels200ResponseInner>>> {
        const requestOptions = await this.getPublicModelsRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(GetPublicModels200ResponseInnerFromJSON));
    }

    /**
     * Returns all active (non-deprecated) LLM and image-generation models available in Fetch Hive as a flat array. Excludes embedding-only models.  `provider` identifies the underlying model maker (e.g. `openai`, `anthropic`, `minimaxai`).  `model_type` is `\"llm\"` for chat/text models and `\"image_generation\"` for models that generate images. `is_vision` means a model accepts image input; `is_image_generation` means a model generates images.  `is_reasoning` is `true` when the model supports reasoning capabilities. 
     * List active models
     */
    async getPublicModels(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<GetPublicModels200ResponseInner>> {
        const response = await this.getPublicModelsRaw(initOverrides);
        return await response.value();
    }

}
